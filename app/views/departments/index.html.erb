<button type="button" class="close" data-dismiss="alert">&times;</button>
	<p><%= flash[:notice] %></p>
</div>

<div class="container-fluid">
	<div class="row-fluid">
		<div class="span2">
			<% @departments.each do |d| %>
  			<p><%= link_to d.department_name, department_path(d) %></p>
			<% end %>
		</div>
		<div class="span10">
			<% @products.shuffle.each do |p| %>
				<div class="product-i">
					<ul class="thumbnails">
						<li class="span3">
							<%= image_tag p.image %> 
						</li>
					</ul>
					<p><%= p.price %></p>
					<p><%= link_to p.name, product_path(p.slug) %></p>
				</div>
			<% end %>
		</div>
	</div>
</div>