<%= form_for :payment, :url => { :action => "create" } do |f| %>
  
  <%= f.label(:first_name) %>
  <%= f.text_field(:first_name) %>

    <button class="btn">
            <%= f.submit %>
          </button>

<% end %>