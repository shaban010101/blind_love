<%= form_for :product, :url => { :action => "sizing_update" }, :method => :put do |form| %>
  <%= fields_for :sizings do |size|%>
      
      <%= size.select :quantity, options_for_select(1..20) %>
  <% end %>
<% end %>